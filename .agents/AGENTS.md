# Workspace Rules

- **Git Synchronization**: Siempre subir y sincronizar los cambios realizados en la base de código al repositorio remoto de GitHub (`git add`, `git commit -m "..."` y `git push origin main`) al finalizar cada tarea.
